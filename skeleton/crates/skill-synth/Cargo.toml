[package]
name = "skill-synth"
version = "0.0.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
sha2 = "0.10"

[dev-dependencies]
jsonschema = { workspace = true }
