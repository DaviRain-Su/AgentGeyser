[package]
name = "skill-synth"
version = "0.0.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
jsonschema = { workspace = true }
